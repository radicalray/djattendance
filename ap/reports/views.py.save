# from reportlab.pdfgen import canvas
from django.http import HttpResponse

def RosterReport(request):
    # Create the HttpResponse objeect with the appropriate PDF headers
e    response = HttpResponse(content_type='application/pdf')
    response['Content-Disposition'] = 'attachment; filename="testfile.pdf"'

    # Create the PDF object, using the response object as its "file."
    p = canvas.Canvas(response)

    # Draw things on the PDF

    p.drawString(100, 100, "Hello world.")

    # Close the PDF object cleanly
    p.showPage()
    p.save()
    return response
